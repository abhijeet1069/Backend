spring:
  application:
    name: spring-jpa-transactions

  datasource:
    url: jdbc:postgresql://localhost:5432/satyam
    username: satyam_spring
    password: satyamx

  jpa:
    hibernate:
      ddl-auto: validate

    show: true

    properties:
      hibernate:
        format_sql: true